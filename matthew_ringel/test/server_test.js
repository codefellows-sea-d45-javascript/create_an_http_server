var chai = require('chai');
var chaihttp = require('chai-http');
chai.use(chaihttp);
var expect = chai.expect;
var fs = require('fs');
require(__dirname + '/../index');

describe('our server', function() {

  it('should respond to a request to /time', function() {
    
  } )
})
